const config = {
    websocket: {
        url: 'http://localhost:8080/ws', // default url, adjust if needed
        heartbeatIncoming: 10000,
        heartbeatOutgoing: 10000
    }
};

export default config;
